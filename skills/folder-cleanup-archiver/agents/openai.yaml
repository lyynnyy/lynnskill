interface:
  display_name: "Folder Cleanup Archiver / 文件清理归档助手"
  short_description: "安全扫描整理本地文件夹并维护归档清理检索索引和任务清单"
  default_prompt: "Use $folder-cleanup-archiver to scan a local folder, identify cleanup and archive candidates, and maintain a searchable cleanup index."
policy:
  allow_implicit_invocation: true
